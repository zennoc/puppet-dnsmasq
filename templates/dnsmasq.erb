#File Managed by Puppet
<% scope.lookupvar('dnsmasq::array_listen').each do |listen| %>listen-address=<%= listen %>
<% end %>
<% scope.lookupvar('dnsmasq::array_servers').each do |dns| %>server=<%= dns %>
<% end %>
cache-size=<%= cache_size %>
<% if bool_no_negcache %>
no-negcache
<% end %>